const names = ['Alice', 'Bob', 'Charlie'];
const newAge = [16, 102, 88];
// const result = names.map((name, i) => ({name : name, age : newAge[i]})); // длинная запись
const result = names.map((name, i) => ({name, age : newAge[i]}));

console.log(result);

let str = 'sderafwd23';

const obj = {
    // lastname : str
    str
};

obj.Age = 11

console.log(obj);
