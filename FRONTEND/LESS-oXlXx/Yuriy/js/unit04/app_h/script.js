function divideString() {
    let input = document.getElementById('').value;

    let first = '';
    let second = '';

    let p = input.indexOf(' ');

    he = input.substring(0, p);
    wo = input.substring(p, Infinity);
}
